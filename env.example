# Email Configuration for Nodemailer
# Copy this file to .env.local and fill in your details.
# IMPORTANT: .env.local is ignored by Git and should NOT be committed.

# Your SMTP server host (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.example.com

# Your SMTP server port (e.g., 587 for TLS, 465 for SSL)
SMTP_PORT=587

# Your SMTP username (usually your full email address)
SMTP_USER=user@example.com

# Your SMTP password (or app-specific password)
SMTP_PASS=your-secret-password

# The email address that will receive the form submissions
SMTP_TO_EMAIL=info@autogalerie-nord.de

# The email address that will appear in the 'From' field of the email.
# It's often required that this is the same as SMTP_USER.
SMTP_FROM_EMAIL=noreply@example.com

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Meta (Facebook) Pixel ID
NEXT_PUBLIC_FB_PIXEL_ID=123456789012345

